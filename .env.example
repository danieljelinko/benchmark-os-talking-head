# TTS Backend Configuration
# Choose between 'coqui' (high quality, slower) or 'piper' (fast, lightweight)
TTS_BACKEND=coqui

# Piper Voice Model Path
# Only used if TTS_BACKEND=piper
PIPER_VOICE=$HOME/.cache/piper/en_US-lessac-medium.onnx

# CUDA Device Selection
# Set to specific GPU index if you have multiple GPUs (e.g., 0, 1, 2)
# Leave commented to use all available GPUs
CUDA_VISIBLE_DEVICES=0
